<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ledinhkhang.qltv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QLTV">
        <activity android:name=".ledinhkhang_activityLogin">

        </activity>
        <activity android:name=".ledinhkhang_activitymain" />
        <activity android:name=".ledinhkhang_activityUpdate" />
        <activity
            android:name=".ledinhkhang_splashActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.QLTV.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>